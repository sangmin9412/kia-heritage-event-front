version: "3.8"
services:
  kia-heritage-front-5500:
    build:
      context: .
      dockerfile: Dockerfile
      no_cache: true
    ports:
      - "5500:5500"
    container_name: kia-heritage-front-5500
    restart: unless-stopped
    environment:
      - PORT=5500
